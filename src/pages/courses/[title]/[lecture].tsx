import { FC } from "react";
import { Course } from "../../../models/Course";

interface LectureProps {
  course: Course;
}

const Lecture: FC<LectureProps> = ({ course }) => {
  return <></>;
};

export default Lecture;
